<UserControl x:Class="EngineeringDiscovery.Wpf.Views.ProductDefinitionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Height="480" Width="800">
    <Grid Background="Transparent" Margin="24">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="760">
            <TextBlock Text="Product Definition" FontSize="26" FontWeight="Bold" Foreground="White" TextAlignment="Center" />

            <Border Background="#202020" Padding="10" Margin="0,10,0,10">
                <StackPanel>
                    <TextBlock Text="Original idea" FontWeight="SemiBold" Foreground="#FFC0C0C0" FontSize="13" />
                    <TextBlock x:Name="IdeaTextBlock" TextWrapping="Wrap" Margin="0,6,0,0" />
                </StackPanel>
            </Border>

            <!-- Primary question — emphasized for readability on dark backgrounds -->
            <TextBlock x:Name="QuestionTextBlock" TextWrapping="Wrap" FontSize="18" FontWeight="SemiBold" Foreground="#FFFFFFFF" Margin="0,6,0,10" />

            <TextBox x:Name="AnswerTextBox" AcceptsReturn="True" Height="160" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button x:Name="PreviousButton" Content="Previous" Width="100" Height="34" Margin="0,0,8,0" Click="PreviousButton_Click" />
                <Button x:Name="ContinueButton" Content="Continue →" Width="140" Height="34" Click="ContinueButton_Click" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>

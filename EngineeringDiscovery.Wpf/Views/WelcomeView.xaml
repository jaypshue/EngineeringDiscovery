<UserControl x:Class="EngineeringDiscovery.Wpf.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource BackgroundBrush}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/DarkTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="3*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="EngineOS" Style="{StaticResource WelcomeTitle}" />
            <TextBlock Text="Engineering is understanding." Style="{StaticResource WelcomeSubtitle}" />
        </StackPanel>

        <Grid Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Top" Width="800">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="32" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="Corporate Engineering" Foreground="{StaticResource PrimaryTextBrush}" FontWeight="Bold" FontSize="18" />
                    <TextBlock Text="Join an existing engineering effort." Foreground="{StaticResource SecondaryTextBrush}" Margin="0,4,0,8" />
                    <TextBlock Text="Recover understanding from an existing codebase and accelerate development." Foreground="{StaticResource SecondaryTextBrush}" TextWrapping="Wrap" Margin="0,0,0,12" />
                    <Button Style="{StaticResource PrimaryButton}" Name="CorporateButton">Open Repository</Button>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="Free Range Engineering" Foreground="{StaticResource PrimaryTextBrush}" FontWeight="Bold" FontSize="18" />
                    <TextBlock Text="Start with an idea." Foreground="{StaticResource SecondaryTextBrush}" Margin="0,4,0,8" />
                    <TextBlock Text="Discover the product, architecture, and implementation before writing code." Foreground="{StaticResource SecondaryTextBrush}" TextWrapping="Wrap" Margin="0,0,0,12" />
                    <Button Style="{StaticResource PrimaryButton}" Name="FreeRangeButton">Start Building</Button>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>

<UserControl x:Class="EngineeringDiscovery.Wpf.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource BackgroundBrush}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/DarkTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="EngineOS" Style="{StaticResource WelcomeTitle}" />
            <TextBlock Text="Engineering is understanding." Style="{StaticResource WelcomeSubtitle}" />
            <TextBlock Text="Where does your engineering begin?" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" Margin="0,8,0,0" />
        </StackPanel>

        <Grid Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Top" Width="880">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="40" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                <StackPanel Margin="8">
                    <TextBlock Text="Corporate Engineering" Foreground="{StaticResource PrimaryTextBrush}" FontWeight="Bold" FontSize="18" />
                    <TextBlock Text="Join an existing engineering effort." Foreground="{StaticResource SecondaryTextBrush}" Margin="0,6,0,12" />
                    <TextBlock Text="Recover understanding from an existing codebase and accelerate development." Foreground="{StaticResource SecondaryTextBrush}" TextWrapping="Wrap" Margin="0,0,0,18" />
                    <Button Style="{StaticResource PrimaryButton}" Name="CorporateButton" HorizontalAlignment="Left" Click="CorporateButton_Click">Open Repository</Button>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                <StackPanel Margin="8">
                    <TextBlock Text="Free Range Engineering" Foreground="{StaticResource PrimaryTextBrush}" FontWeight="Bold" FontSize="18" />
                    <TextBlock Text="Independent product development." Foreground="{StaticResource SecondaryTextBrush}" Margin="0,6,0,12" />
                    <TextBlock Text="Discover the product, architecture, and implementation before writing code." Foreground="{StaticResource SecondaryTextBrush}" TextWrapping="Wrap" Margin="0,0,0,18" />
                    <Button Style="{StaticResource PrimaryButton}" Name="FreeRangeButton" HorizontalAlignment="Right" Click="FreeRangeButton_Click">Start Building</Button>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
